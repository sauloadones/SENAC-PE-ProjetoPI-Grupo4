<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/rounded_popup"
    android:padding="24dp"
    android:layout_gravity="center">

    <ImageView
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@drawable/logo"
        android:layout_gravity="center_horizontal"
        android:contentDescription="Logo" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Esqueci a senha"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="8dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="1. Ao inserir seu ID iremos buscar qual e-mail está vinculado ao ID cadastrado;\n2. Em caso de perda de acesso ao e-mail cadastrado por favor solicitar ajuda à empresa ou contatar o suporte\n3. Será enviado um link de recuperação de acesso para o seu email onde será possível criar uma nova senha;\n4. Após recuperar sua senha basta logar novamente."
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:layout_marginTop="12dp" />

    <EditText
        android:id="@+id/editTextIdRecuperacao"
        android:layout_width="280dp"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:hint="ID"
        android:textColor="#FFFFFF"
        android:textColorHint="#EAEAEA"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:background="@drawable/rounded_input"
        android:layout_gravity="center_horizontal" />

    <Button
        android:id="@+id/buttonRecuperar"
        android:layout_width="280dp"
        android:layout_height="48dp"
        android:text="Entrar"
        android:layout_marginTop="24dp"
        android:backgroundTint="#164773"
        android:textColor="#FFFFFF"
        android:layout_gravity="center_horizontal" />
</LinearLayout>
