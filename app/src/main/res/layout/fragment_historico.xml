<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@drawable/background_gradient"
  tools:context=".HistoricoFragment">

  <!-- TopBar padrão -->
  <include
    android:id="@+id/topBar"
    layout="@layout/view_top_bar"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:layout_constraintTop_toTopOf="parent" />

  <!-- Título "Histórico de Pilar" -->
  <TextView
    android:id="@+id/tituloHistorico"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Histórico de Pilar"
    android:textColor="@color/branco"
    android:textSize="22sp"
    android:textStyle="bold"
    android:layout_marginStart="16dp"
    android:layout_marginTop="12dp"
    app:layout_constraintTop_toBottomOf="@id/topBar"
    app:layout_constraintStart_toStartOf="parent" />

  <!-- Botão "Todos" -->
  <TextView
    android:id="@+id/botaoTodos"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_filtro_botao"
    android:text="Todos"
    android:textColor="@color/branco"
    android:textSize="14sp"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp"
    android:layout_marginTop="8dp"
    android:layout_marginEnd="8dp"
    android:clickable="true"
    android:focusable="true"
    app:layout_constraintTop_toBottomOf="@id/topBar"
    app:layout_constraintEnd_toStartOf="@id/botaoFiltro" />

  <!-- Botão de filtro (ícone) -->
  <ImageView
    android:id="@+id/botaoFiltro"
    android:layout_width="36dp"
    android:layout_height="36dp"
    android:padding="8dp"
    android:background="@drawable/bg_filtro_botao"
    android:src="@drawable/ic_filtro"
    android:layout_marginTop="8dp"
    android:layout_marginEnd="16dp"
    android:clickable="true"
    android:focusable="true"
    app:layout_constraintTop_toBottomOf="@id/topBar"
    app:layout_constraintEnd_toEndOf="parent"
    app:tint="@color/branco" />

  <!-- RecyclerView de históricos -->
  <androidx.recyclerview.widget.RecyclerView
    android:id="@+id/recyclerHistoricoPilares"
    android:layout_width="0dp"
    android:layout_height="0dp"
    android:clipToPadding="false"
    android:paddingTop="8dp"
    android:paddingBottom="16dp"
    app:layout_constraintTop_toBottomOf="@id/tituloHistorico"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent" />

  <!-- Empty State -->
  <TextView
    android:id="@+id/emptyStateView"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:text="Nenhum pilar foi encontrado"
    android:textColor="#CCCCCC"
    android:gravity="center"
    android:textSize="14sp"
    android:padding="32dp"
    android:visibility="gone"
    app:layout_constraintTop_toTopOf="@id/recyclerHistoricoPilares"
    app:layout_constraintBottom_toBottomOf="@id/recyclerHistoricoPilares"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>
